# SYNESIS-DEDALA-EXPERTISE-TARGETED-REPAIR-001

## Authority and scope

Founder-authorized exceptional Sýnesis execution of the targeted material repair raised by `DEDALA-EXPERTISE-ADVERSARIAL-SELF-ASSURANCE-001`.

This is an implementation/reverification operation only. It does not create constitution, change OCS identity, expand authority, promote expert material to canon, or issue global PASS.

## Targeted repairs

1. `RetrievalPlan` now carries `allowed_source_families` derived from selected expert lenses.
2. Retrieval rejects evidence from source families outside those typed lens bindings even when lexical tags overlap.
3. `ExpertSourceRecord` carries an explicit `source_family` plus a SHA-256 `provenance_hash` over provenance-bearing fields, including `content_hash`, locator, tags, source ID and family. Validation recomputes the digest.
4. Boolean falsification/verification callbacks are no longer admissible. The physiology requires evaluator objects that emit typed `FalsificationReceipt` and `VerificationReceipt` values bound to candidate, evidence, assessed revision, procedure and evidence refs.
5. The causal claim is narrowed to `ROUTING_AND_EVIDENCE_ATTACHMENT_CAUSALITY = PROVEN_IN_BOUNDED_HARNESS`.
6. `ARCHITECTURAL_DERIVATION_SEMANTIC_CAUSALITY = NOT_YET_PROVEN` remains explicit.
7. Adversarial tests cover wrong-family lexical overlap, provenance-field tampering, boolean callback rejection, wrong revision/candidate/evidence receipt bindings, missing expected family and bounded counterfactual failure.

## Preserved invariants

- `SOURCE != AUTHORITY`
- `DERIVATION != CANON`
- `LEARNING != PROMOTION`
- identity effect = `NONE`
- authority effect = `NONE`
- canon effect = `NONE`
- governance evidence is not silently replaced by expert evidence
- material execution remains behind the Universal Kernel

## Non-claims

- no universal architectural truth
- no real-world superiority proof
- no independent assurance claim by Dédala
- no semantic derivation causality claim
- no foundational reopening
- no C09/C10 PASS

## Required closure evidence

The repair is closable only after the dedicated Dédala expertise workflow and repository Quality Gate succeed on the exact PR head, the exact head is merged without drift, and the post-merge Quality Gate succeeds on the resulting merge commit.
